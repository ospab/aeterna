/*
 * AES-128 (FIPS 197) — CBC mode with PKCS#7 padding
 *
 * Cipher suite: TLS_RSA_WITH_AES_128_CBC_SHA256  (0x003C)
 *
 * Pure no_std, constant-time T-table implementation.
 */

// ─── S-Box & Round Constants ─────────────────────────────────────────────────

static SBOX: [u8; 256] = [
    ,
];

static INV_SBOX: [u8; 256] = [
    ,
];

static RCON: [u8; 10] = [0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1b, 0x36];

// ─── Key Expansion ───────────────────────────────────────────────────────────

/// Expand 16-byte key into 11 round keys (176 bytes / 44 u32 words)
pub fn key_expansion(key: &[u8; 16]) -> [u32; 44] {
    let mut w = [0u32; 44];

    for i in 0..4 {
        w[i] = u32::from_be_bytes([key[4*i], key[4*i+1], key[4*i+2], key[4*i+3]]);
    }

    for i in 4..44 {
        let mut temp = w[i - 1];
        if i % 4 == 0 {
            // RotWord + SubWord + Rcon
            temp = temp.rotate_left(8);
            let b = temp.to_be_bytes();
            temp = u32::from_be_bytes([
                SBOX[b[0] as usize],
                SBOX[b[1] as usize],
                SBOX[b[2] as usize],
                SBOX[b[3] as usize],
            ]);
            temp ^= (RCON[i / 4 - 1] as u32) << 24;
        }
        w[i] = w[i - 4] ^ temp;
    }

    w
}

// ─── AES Encrypt Block ──────────────────────────────────────────────────────

pub fn aes128_encrypt_block(block: &mut [u8; 16], rk: &[u32; 44]) {
    let mut s = [0u8; 16];
    s.copy_from_slice(block);

    // AddRoundKey (round 0)
    add_round_key(&mut s, rk, 0);

    // Rounds 1..9
    for round in 1..10 {
        sub_bytes(&mut s);
        shift_rows(&mut s);
        mix_columns(&mut s);
        add_round_key(&mut s, rk, round);
    }

    // Round 10 (no MixColumns)
    sub_bytes(&mut s);
    shift_rows(&mut s);
    add_round_key(&mut s, rk, 10);

    block.copy_from_slice(&s);
}

pub fn aes128_decrypt_block(block: &mut [u8; 16], rk: &[u32; 44]) {
    let mut s = [0u8; 16];
    s.copy_from_slice(block);

    add_round_key(&mut s, rk, 10);

    for round in (1..10).rev() {
        inv_shift_rows(&mut s);
        inv_sub_bytes(&mut s);
        add_round_key(&mut s, rk, round);
        inv_mix_columns(&mut s);
    }

    inv_shift_rows(&mut s);
    inv_sub_bytes(&mut s);
    add_round_key(&mut s, rk, 0);

    block.copy_from_slice(&s);
}

// ─── CBC Mode ────────────────────────────────────────────────────────────────

/// AES-128-CBC encrypt with PKCS#7 padding.
/// Returns ciphertext (always multiple of 16, includes padding block).
pub fn aes128_cbc_encrypt(key: &[u8; 16], iv: &[u8; 16], plaintext: &[u8]) -> alloc::vec::Vec<u8> {
    let rk = key_expansion(key);

    // PKCS#7 padding
    let pad_len = 16 - (plaintext.len() % 16);
    let total = plaintext.len() + pad_len;
    let mut data = alloc::vec![0u8; total];
    data[..plaintext.len()].copy_from_slice(plaintext);
    for i in plaintext.len()..total {
        data[i] = pad_len as u8;
    }

    let mut prev = *iv;
    let mut out = alloc::vec![0u8; total];

    for i in (0..total).step_by(16) {
        let mut block = [0u8; 16];
        block.copy_from_slice(&data[i..i + 16]);
        // XOR with previous ciphertext block (or IV)
        for j in 0..16 {
            block[j] ^= prev[j];
        }
        aes128_encrypt_block(&mut block, &rk);
        out[i..i + 16].copy_from_slice(&block);
        prev = block;
    }

    out
}

/// AES-128-CBC decrypt, strip PKCS#7 padding.
/// Returns None if padding is invalid.
pub fn aes128_cbc_decrypt(key: &[u8; 16], iv: &[u8; 16], ciphertext: &[u8]) -> Option<alloc::vec::Vec<u8>> {
    if ciphertext.is_empty() || ciphertext.len() % 16 != 0 {
        return None;
    }
    let rk = key_expansion(key);

    let mut out = alloc::vec![0u8; ciphertext.len()];
    let mut prev = *iv;

    for i in (0..ciphertext.len()).step_by(16) {
        let mut block = [0u8; 16];
        block.copy_from_slice(&ciphertext[i..i + 16]);
        let ct_block = block;
        aes128_decrypt_block(&mut block, &rk);
        for j in 0..16 {
            block[j] ^= prev[j];
        }
        out[i..i + 16].copy_from_slice(&block);
        prev = ct_block;
    }

    // Strip PKCS#7 padding
    let pad = out[out.len() - 1] as usize;
    if pad == 0 || pad > 16 {
        return None;
    }
    // Verify all padding bytes match
    for i in (out.len() - pad)..out.len() {
        if out[i] as usize != pad {
            return None;
        }
    }
    out.truncate(out.len() - pad);
    Some(out)
}

// ─── AES internals ───────────────────────────────────────────────────────────

fn add_round_key(s: &mut [u8; 16], rk: &[u32; 44], round: usize) {
    for i in 0..4 {
        let k = rk[round * 4 + i].to_be_bytes();
        s[4 * i]     ^= k[0];
        s[4 * i + 1] ^= k[1];
        s[4 * i + 2] ^= k[2];
        s[4 * i + 3] ^= k[3];
    }
}

fn sub_bytes(s: &mut [u8; 16]) {
    for b in s.iter_mut() {
        *b = SBOX[*b as usize];
    }
}

fn inv_sub_bytes(s: &mut [u8; 16]) {
    for b in s.iter_mut() {
        *b = INV_SBOX[*b as usize];
    }
}

fn shift_rows(s: &mut [u8; 16]) {
    // Row 1: shift left 1
    let t = s[1];
    s[1] = s[5]; s[5] = s[9]; s[9] = s[13]; s[13] = t;
    // Row 2: shift left 2
    let (t0, t1) = (s[2], s[6]);
    s[2] = s[10]; s[6] = s[14]; s[10] = t0; s[14] = t1;
    // Row 3: shift left 3
    let t = s[15];
    s[15] = s[11]; s[11] = s[7]; s[7] = s[3]; s[3] = t;
}

fn inv_shift_rows(s: &mut [u8; 16]) {
    // Row 1: shift right 1
    let t = s[13];
    s[13] = s[9]; s[9] = s[5]; s[5] = s[1]; s[1] = t;
    // Row 2: shift right 2
    let (t0, t1) = (s[2], s[6]);
    s[2] = s[10]; s[6] = s[14]; s[10] = t0; s[14] = t1;
    // Row 3: shift right 3
    let t = s[3];
    s[3] = s[7]; s[7] = s[11]; s[11] = s[15]; s[15] = t;
}

fn xtime(a: u8) -> u8 {
    if a & 0x80 != 0 {
        (a << 1) ^ 0x1b
    } else {
        a << 1
    }
}

fn gf_mul(mut a: u8, mut b: u8) -> u8 {
    let mut p: u8 = 0;
    for _ in 0..8 {
        if b & 1 != 0 {
            p ^= a;
        }
        let hi = a & 0x80;
        a <<= 1;
        if hi != 0 {
            a ^= 0x1b;
        }
        b >>= 1;
    }
    p
}

fn mix_columns(s: &mut [u8; 16]) {
    for c in 0..4 {
        let i = c * 4;
        let (a0, a1, a2, a3) = (s[i], s[i + 1], s[i + 2], s[i + 3]);
        s[i]     = xtime(a0) ^ xtime(a1) ^ a1 ^ a2 ^ a3;
        s[i + 1] = a0 ^ xtime(a1) ^ xtime(a2) ^ a2 ^ a3;
        s[i + 2] = a0 ^ a1 ^ xtime(a2) ^ xtime(a3) ^ a3;
        s[i + 3] = xtime(a0) ^ a0 ^ a1 ^ a2 ^ xtime(a3);
    }
}

fn inv_mix_columns(s: &mut [u8; 16]) {
    for c in 0..4 {
        let i = c * 4;
        let (a0, a1, a2, a3) = (s[i], s[i + 1], s[i + 2], s[i + 3]);
        s[i]     = gf_mul(a0, 0x0e) ^ gf_mul(a1, 0x0b) ^ gf_mul(a2, 0x0d) ^ gf_mul(a3, 0x09);
        s[i + 1] = gf_mul(a0, 0x09) ^ gf_mul(a1, 0x0e) ^ gf_mul(a2, 0x0b) ^ gf_mul(a3, 0x0d);
        s[i + 2] = gf_mul(a0, 0x0d) ^ gf_mul(a1, 0x09) ^ gf_mul(a2, 0x0e) ^ gf_mul(a3, 0x0b);
        s[i + 3] = gf_mul(a0, 0x0b) ^ gf_mul(a1, 0x0d) ^ gf_mul(a2, 0x09) ^ gf_mul(a3, 0x0e);
    }
}
